# GPU-only dependencies for scripts that must run on Colab (or any CUDA
# machine) rather than the local Mac dev environment — bitsandbytes 4-bit
# quantization needs CUDA, it doesn't work on Mac/MPS.
#
# Usage on Colab: pip install -r requirements.txt -r requirements-colab.txt

transformers
accelerate
bitsandbytes
peft

# M5: GPTQ post-training quantization (train/quantize_gptq.py) — optimum
# wraps the calibration/quantization flow, gptqmodel is the maintained
# kernel backend (replaces the now-stale auto-gptq). See
# docs/grilling_m5_pre.md Q2.
optimum
gptqmodel

# M6: vLLM serving benchmark (eval/generate_vllm.py) — see docs/grilling_m6_pre.md
vllm
