__pycache__/
*.pyc
.pytest_cache/
.venv/
*.egg-info/
.DS_Store

# secrets / local credentials — never commit
.env
.env.*
!.env.example

# M3 training intermediate output (checkpoints, logs) — final adapter goes to models/lora_adapter/ and IS committed
train/output/
