__pycache__/
*.pyc
.pytest_cache/
.venv/
*.egg-info/
.DS_Store

# secrets / local credentials — never commit
.env
.env.*
!.env.example

# M3 training intermediate output (checkpoints, logs) — final adapter goes to models/lora_adapter/ and IS committed
train/output/

# M5 quantization artifacts (GB-scale, regenerable via train/quantize_gptq.py) —
# only models/lora_adapter/ (M3's small LoRA adapter) is committed
models/merged_fp16/
models/gptq_4bit/
